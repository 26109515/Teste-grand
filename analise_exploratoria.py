import pandas as pd\nimport matplotlib.pyplot as plt\nimport seaborn as sns\n\n# Load dataset\ndf = pd.read_csv('data.csv')  # Change to your dataset path\n\n# 1. Display the first rows\nprint(df.head())\n\n# 2. Check column data types\nprint(df.dtypes)\n\n# 3. Check for null values\nprint(df.isnull().sum())\n\n# 4. Churn distribution statistics\nchurn_statistics = df['churn'].describe()\nprint(churn_statistics)\n\n# 5. Descriptive statistics for all columns\nprint(df.describe())\n\n# Visualizations\n# Set the output directory\noutput_dir = 'outputs/charts/'\n\n# 1. Churn distribution\nsns.countplot(x='churn', data=df)\nplt.title('Churn Distribution')\nplt.savefig(output_dir + 'churn_distribution.png')\nplt.clf()\n\n# 2. Total time by churn\nsns.boxplot(x='churn', y='time_total', data=df)\nplt.title('Total Time by Churn')\nplt.savefig(output_dir + 'time_total_by_churn.png')\nplt.clf()\n\n# 3. Error count by churn\nsns.boxplot(x='churn', y='qtd_erros', data=df)\nplt.title('Quantity of Errors by Churn')\nplt.savefig(output_dir + 'qtd_erros_by_churn.png')\nplt.clf()\n\n# 4. Useless clicks by churn\nsns.boxplot(x='churn', y='qtd_cliques_inuteis', data=df)\nplt.title('Quantity of Useless Clicks by Churn')\nplt.savefig(output_dir + 'qtd_cliques_inuteis_by_churn.png')\nplt.clf()\n\nplt.show()